[package]
name = "palmcds"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]

[dev-dependencies]
criterion = "0.5"
petgraph = "0.6"

[[bench]]
name = "traversal"
harness = false
